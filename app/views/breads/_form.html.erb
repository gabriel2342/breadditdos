<%= form_with(model: bread) do |form| %>
  <% if bread.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bread.errors.count, "error") %> prohibited this bread from being saved:</h2>

      <ul>
        <% bread.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :story, style: "display: block" %>
    <%= form.text_area :story %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
